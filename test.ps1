Write-Host "DEBUG: proc is null? $($null -eq $proc)" -ForegroundColor Yellow
Write-Host "DEBUG: proc type = $($proc.GetType().FullName)" -ForegroundColor Yellow
Write-Host "DEBUG: proc id = $($proc.Id)" -ForegroundColor Yellow
